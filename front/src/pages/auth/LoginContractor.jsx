import LoginCard from "./LoginCard";

export default function LoginContractor() {
    return (
        <LoginCard
            role="Contractor"
            accentColor="#d97706"
            icon="🏗️"
            description="Manage projects, teams, site visits, and progress tracking."
        />
    );
}

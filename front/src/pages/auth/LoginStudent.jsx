import LoginCard from "./LoginCard";

export default function LoginStudent() {
    return (
        <LoginCard
            role="Student"
            accentColor="#7c3aed"
            icon="🎓"
            description="Learn, estimate, and explore construction projects as a student."
        />
    );
}
